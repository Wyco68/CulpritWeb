# better-auth declares optional peers (e.g. @sveltejs/kit) that hoist a conflicting
# vite version against @vitejs/plugin-react. We don't use those frameworks, so relax
# strict peer resolution. Vercel reads this file too, keeping local + CI installs identical.
legacy-peer-deps=true
