// integrations module — edges behind interfaces: Calendly (embed only, no sync), StorageAdapter
// (R2 / Supabase), EmailClient (Resend), TurnstileVerifier, RateLimiter (Upstash). No domain logic.
export {};
