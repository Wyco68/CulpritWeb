// notifications module — React Email templates + Resend; one email per appointment transition
// (Received / Approved / Declined / Booked / Cancelled). Email failure never rolls back state.
export {};
